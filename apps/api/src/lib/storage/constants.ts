/** Maximum row limit for analytics export queries to prevent memory exhaustion */
export const MAX_EXPORT_SIZE = 500;

export const MAX_USAGE_EVENTS = 500;
export const MAX_PAYMENT_ATTEMPTS = 500;
export const DEFAULT_RECENT_LIMIT = 10;

